import React from 'react';

export default function FaqSchema(): React.ReactNode {
  // FAQPage structured data is centrally emitted by <InternalSEO /> in Layout.tsx
  // to avoid duplicate JSON-LD schema blocks.
  return null;
}

export { FaqSchema };
