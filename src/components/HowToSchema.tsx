import React from 'react';

export default function HowToSchema(): React.ReactNode {
  // HowTo structured data is centrally emitted by <InternalSEO /> in Layout.tsx
  // to avoid duplicate JSON-LD schema blocks.
  return null;
}

export { HowToSchema };
